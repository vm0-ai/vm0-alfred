version: "1.0"

agents:
  alfred:
    description: "Alfred - AI-powered assistant agent"
    provider: claude-code
    image: alfred
    beta_system_prompt: AGENTS.md
    beta_system_skills: []
    environment:
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
